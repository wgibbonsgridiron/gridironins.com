---
const base = import.meta.env.BASE_URL;
---
<section class="max-w-7xl mx-auto px-4 py-16 bg-white dark:bg-slate-950 transition-colors">
  <div class="rounded-2xl bg-gradient-to-br from-brand-600 to-brand-900 dark:from-brand-700 dark:to-brand-950 p-8 md:p-12 relative overflow-hidden shadow-2xl">
    <div class="absolute inset-0 bg-[radial-gradient(circle_at_30%_50%,rgba(255,255,255,0.15),transparent)]"></div>
    <div class="relative z-10 flex flex-col md:flex-row items-start md:items-center justify-between gap-6">
      <div class="max-w-2xl">
        <h3 class="text-3xl md:text-4xl font-bold text-white mb-3">Move fast with clarity.</h3>
        <p class="text-lg text-brand-50 leading-relaxed">Start a submission or review a quote in minutes. Experience modern insurance infrastructure built for your workflow.</p>
      </div>
      <div class="flex flex-col sm:flex-row gap-4 flex-shrink-0">
        <a href="https://app.gridironins.com" class="inline-flex items-center justify-center rounded-lg px-8 py-3 bg-white text-brand-600 hover:bg-brand-50 font-semibold shadow-lg hover:shadow-xl transition-all">
          <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z"></path>
          </svg>
          Launch App
        </a>
        <a href={`${base}contact`} class="inline-flex items-center justify-center rounded-lg px-8 py-3 border-2 border-white text-white hover:bg-white/10 font-semibold transition-all">
          Contact Sales
        </a>
      </div>
    </div>
  </div>
</section>
