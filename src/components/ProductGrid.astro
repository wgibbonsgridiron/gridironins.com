---
const products = [
  { slug: 'inland-marine', name: 'Inland Marine', blurb: 'Bailees, scheduled property floater, electronic/medical equipment, builders risk, contractors equipment.' },
  { slug: 'excess-liability', name: 'Excess Liability', blurb: 'Follow-form excess built for speed with clear terms.' },
  { slug: 'property', name: 'Property', blurb: 'Cat-exposed property with transparent pricing and controls.' },
  { slug: 'primary-flood', name: 'Primary Flood', blurb: 'Primary flood with geospatial precision and modern UX.' },
  { slug: 'contractors-equipment', name: 'Contractors Equipment / Owners Interest', blurb: 'Field-ready coverage with granular scheduling.' }
];
---
<section class="max-w-7xl mx-auto px-4 py-16">
  <h2 class="text-2xl md:text-3xl font-semibold text-slate-900 dark:text-slate-100">Products</h2>
  <p class="mt-2 text-slate-700 dark:text-slate-300 max-w-2xl">Commercial products with quoting, binding, and issuance on a modern platform.</p>
  <div class="mt-8 grid gap-6 md:grid-cols-2 lg:grid-cols-3">
    {products.map(p => (
      <a href={`/products/${p.slug}`} class="group rounded-xl border border-slate-200 dark:border-slate-700 bg-white dark:bg-slate-800 p-6 hover:shadow-lg hover:border-brand-300 dark:hover:border-brand-600 transition-all duration-300">
        <div class="text-lg font-medium text-slate-900 dark:text-slate-100">{p.name}</div>
        <p class="mt-2 text-slate-600 dark:text-slate-400 text-sm">{p.blurb}</p>
        <span class="mt-4 inline-flex items-center gap-1 text-brand-600 dark:text-brand-400 text-sm font-medium group-hover:gap-2 transition-all">
          Learn more →
        </span>
      </a>
    ))}
  </div>
</section>
