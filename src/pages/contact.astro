---
import Layout from '../layouts/Layout.astro';
---
<Layout title="Contact | Gridiron Insurance" description="Reach our team." url="https://gridironins.com/contact">
  <section class="max-w-3xl mx-auto px-4 py-16">
    <div class="mb-8">
      <h1 class="text-4xl font-bold tracking-tight text-slate-900 dark:text-slate-100">Get in Touch</h1>
      <p class="mt-3 text-lg text-slate-600 dark:text-slate-400">
        Have questions about our products or want to discuss a specific risk? We're here to help.
      </p>
    </div>

    <div class="grid md:grid-cols-2 gap-8 mb-12">
      <div class="p-6 rounded-xl border border-slate-200 dark:border-slate-700 bg-white dark:bg-slate-800">
        <h3 class="font-semibold text-lg mb-2 text-slate-900 dark:text-slate-100">Email</h3>
        <a href="mailto:hello@gridironins.com" class="text-brand-600 dark:text-brand-400 hover:underline">
          hello@gridironins.com
        </a>
      </div>
      <div class="p-6 rounded-xl border border-slate-200 dark:border-slate-700 bg-white dark:bg-slate-800">
        <h3 class="font-semibold text-lg mb-2 text-slate-900 dark:text-slate-100">Phone</h3>
        <a href="tel:+15551234500" class="text-brand-600 dark:text-brand-400 hover:underline">
          (555) 123-4500
        </a>
      </div>
    </div>

    <div class="p-8 rounded-xl border border-slate-200 dark:border-slate-700 bg-white dark:bg-slate-800 shadow-lg">
      <h2 class="text-2xl font-semibold mb-6 text-slate-900 dark:text-slate-100">Send us a message</h2>
      <form class="grid gap-4" onsubmit="return submitForm(event)">
        <label class="grid gap-2">
          <span class="text-sm font-medium text-slate-700 dark:text-slate-300">Name *</span>
          <input name="name" required class="border border-slate-300 dark:border-slate-600 bg-white dark:bg-slate-900 text-slate-900 dark:text-slate-100 rounded-lg px-4 py-2.5 focus:outline-none focus:ring-2 focus:ring-brand-500 dark:focus:ring-brand-400 transition-colors" />
        </label>
        <label class="grid gap-2">
          <span class="text-sm font-medium text-slate-700 dark:text-slate-300">Email *</span>
          <input type="email" name="email" required class="border border-slate-300 dark:border-slate-600 bg-white dark:bg-slate-900 text-slate-900 dark:text-slate-100 rounded-lg px-4 py-2.5 focus:outline-none focus:ring-2 focus:ring-brand-500 dark:focus:ring-brand-400 transition-colors" />
        </label>
        <label class="grid gap-2">
          <span class="text-sm font-medium text-slate-700 dark:text-slate-300">Company</span>
          <input name="company" class="border border-slate-300 dark:border-slate-600 bg-white dark:bg-slate-900 text-slate-900 dark:text-slate-100 rounded-lg px-4 py-2.5 focus:outline-none focus:ring-2 focus:ring-brand-500 dark:focus:ring-brand-400 transition-colors" />
        </label>
        <label class="grid gap-2">
          <span class="text-sm font-medium text-slate-700 dark:text-slate-300">Message *</span>
          <textarea name="message" rows="6" required class="border border-slate-300 dark:border-slate-600 bg-white dark:bg-slate-900 text-slate-900 dark:text-slate-100 rounded-lg px-4 py-2.5 focus:outline-none focus:ring-2 focus:ring-brand-500 dark:focus:ring-brand-400 transition-colors"></textarea>
        </label>
        <button class="rounded-lg px-6 py-3 bg-brand-500 text-white hover:bg-brand-600 dark:bg-brand-600 dark:hover:bg-brand-700 font-medium shadow-soft transition-colors w-full md:w-auto">
          Send Message
        </button>
        <p id="contact-status" class="text-sm text-slate-600 dark:text-slate-400"></p>
      </form>
    </div>

    <div class="mt-12 p-6 rounded-xl bg-slate-50 dark:bg-slate-800 border border-slate-200 dark:border-slate-700">
      <h3 class="font-semibold text-lg mb-3 text-slate-900 dark:text-slate-100">Prefer to speak with someone directly?</h3>
      <p class="text-slate-600 dark:text-slate-400 mb-4">
        Our underwriting team is available Monday-Friday, 9am-5pm EST. For urgent matters, please call our main line.
      </p>
      <div class="flex gap-4">
        <a href="tel:+15551234500" class="inline-flex items-center px-5 py-2.5 rounded-lg border border-slate-300 dark:border-slate-600 hover:border-slate-400 dark:hover:border-slate-500 font-medium transition-colors">
          Call Us
        </a>
        <a href="https://app.gridironins.com" class="inline-flex items-center px-5 py-2.5 rounded-lg bg-brand-500 text-white hover:bg-brand-600 dark:bg-brand-600 dark:hover:bg-brand-700 font-medium transition-colors">
          Launch App
        </a>
      </div>
    </div>

    <script>
      async function submitForm(e: any){
        e.preventDefault();
        const el = document.getElementById('contact-status');
        el!.textContent = 'Submitting...';
        const body = Object.fromEntries(new FormData(e.target));
        const res = await fetch('/api/contact', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(body) });
        el!.textContent = await res.text();
        if(res.ok) e.target.reset();
        return false;
      }
    </script>
  </section>
</Layout>
