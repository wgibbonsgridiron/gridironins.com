---
const products = [
  { slug: 'inland-marine', name: 'Inland Marine', blurb: 'Bailees, scheduled property floater, electronic/medical equipment, builders risk, contractors equipment.' },
  { slug: 'excess-liability', name: 'Excess Liability', blurb: 'Follow-form excess built for speed with clear terms.' },
  { slug: 'property', name: 'Property', blurb: 'Cat-exposed property with transparent pricing and controls.' },
  { slug: 'primary-flood', name: 'Primary Flood', blurb: 'Primary flood with geospatial precision and modern UX.' },
  { slug: 'contractors-equipment', name: 'Contractors Equipment / Owners Interest', blurb: 'Field-ready coverage with granular scheduling.' }
];
---
<section class="max-w-7xl mx-auto px-4 py-16 bg-white dark:bg-slate-950 transition-colors">
  <h2 class="text-2xl md:text-3xl font-bold text-slate-900 dark:text-white">Products</h2>
  <p class="mt-3 text-slate-700 dark:text-slate-300 max-w-2xl text-lg">Commercial products with quoting, binding, and issuance on a modern platform.</p>
  <div class="mt-10 grid gap-6 md:grid-cols-2 lg:grid-cols-3">
    {products.map(p => (
      <a href={`/products/${p.slug}`} class="group rounded-xl border border-slate-200 dark:border-slate-700 bg-white dark:bg-slate-900 p-6 hover:shadow-xl hover:border-brand-400 dark:hover:border-brand-500 dark:hover:shadow-brand-500/10 transition-all duration-300">
        <div class="text-lg font-semibold text-slate-900 dark:text-slate-100">{p.name}</div>
        <p class="mt-3 text-slate-600 dark:text-slate-400 text-sm leading-relaxed">{p.blurb}</p>
        <span class="mt-5 inline-flex items-center gap-1 text-brand-600 dark:text-brand-400 text-sm font-semibold group-hover:gap-2 transition-all">
          Learn more →
        </span>
      </a>
    ))}
  </div>
</section>
